import { IconName } from "@/src/lib/icons";

export interface ValuesAsescon {
  id: number;
  title: string;
  description: string;
  icon: IconName;
  color: string;
  bg: string;
  line?: string;
}
